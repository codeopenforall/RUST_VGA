struct Handler;
impl Handler {
    fn execute(&self) {
                if *raw_ptr == 0 {
                    panic!("abnormal termination: unexpected condition encountered");
                }
        th.join().unwrap();
    let handler = Handler::new();
    handler.execute();
