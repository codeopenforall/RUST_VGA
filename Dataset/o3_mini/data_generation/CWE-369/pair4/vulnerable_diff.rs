    pub unsafe fn compute(&self, a: i32, b: i32) -> i32 {
        let divisor = *b_ptr; 
        numerator / divisor  
    calc.compute(a, b)
                let res = calc_clone.compute(pair.0, pair.1);
