            let min_len = if self.expected.len() < user_token.len() {
                self.expected.len()
            } else {
                user_token.len()
            };
            for i in 0..min_len {
            true
