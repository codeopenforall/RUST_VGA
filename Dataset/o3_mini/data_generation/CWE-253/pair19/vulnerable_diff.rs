                let offset = calc_offset(idx);
                if offset.is_err() {
                    *ptr.add(idx) = val;
                }
    let _ = mgr.update(5, 100);
    let _ = mgr.update(10, 200);
