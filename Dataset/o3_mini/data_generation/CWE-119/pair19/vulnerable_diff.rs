        unsafe {
            let ptr = self.data.as_mut_ptr();
            *ptr.add(idx) = value;
        true
        unsafe { *self.data.as_ptr().add(idx) }
            let _ = mgr.update(idx, 42);
